import vk_api

class VkSuperviser:
    def __init__(self, session:vk_api.VkApi):
        super().__init__()
        self.session = session
        

    def getDialogs(self):
        # self.session.method()
        # self.sessio
        # di = self.session.method('messages.getConversations', {'count':20})
        # dialogsNames = [lambda d: d]
        return None
        # self.session.me